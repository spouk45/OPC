{% extends 'base.html.twig' %}

{% block title %}HeatingType index{% endblock %}

{% block body %}
    <h1>HeatingType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for heating_type in heating_types %}
            <tr>
                <td>{{ heating_type.id }}</td>
                <td>{{ heating_type.name }}</td>
                <td>
                    <a href="{{ path('heating_type_show', {'id': heating_type.id}) }}">show</a>
                    <a href="{{ path('heating_type_edit', {'id': heating_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('heating_type_new') }}">Create new</a>
{% endblock %}
