{% extends 'base.html.twig' %}

{% block title %}HeatingType{% endblock %}

{% block body %}
    <h1>HeatingType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ heating_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ heating_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('heating_type_index') }}">back to list</a>

    <a href="{{ path('heating_type_edit', {'id': heating_type.id}) }}">edit</a>

    {{ include('heating_type/_delete_form.html.twig') }}
{% endblock %}
