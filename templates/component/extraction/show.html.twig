{% extends 'base.html.twig' %}

{% block title %}ExtractionType{% endblock %}

{% block body %}
    <h1>ExtractionType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extraction_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ extraction_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('extraction_type_index') }}">back to list</a>

    <a href="{{ path('extraction_type_edit', {'id': extraction_type.id}) }}">edit</a>

    {{ include('extraction_type/_delete_form.html.twig') }}
{% endblock %}
