{% extends 'base.html.twig' %}

{% block title %}InterventionReport{% endblock %}

{% block body %}
    <h1>InterventionReport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ intervention_report.id }}</td>
            </tr>
            <tr>
                <th>PlannedDate</th>
                <td>{{ intervention_report.plannedDate ? intervention_report.plannedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>RealisedDate</th>
                <td>{{ intervention_report.realisedDate ? intervention_report.realisedDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ intervention_report.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('intervention_report_list',{'customer': intervention_report.customer.id} ) }}">back to list</a>

    <a href="{{ path('intervention_report_edit', {'id': intervention_report.id}) }}">edit</a>

    {{ include('intervention_report/_delete_form.html.twig') }}
{% endblock %}
