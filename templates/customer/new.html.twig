{% extends 'base.html.twig' %}

{% block title %}Nouveau client{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1 class="text-center">Nouveau client</h1>
            </div>
        </div>
        <div class="row">
            <div class="col">
    {{ include('customer/_form.html.twig') }}
            </div>
        </div>
    <a class="btn btn-info" href="{{ path('customer_index') }}">Retour à la liste</a>
    </div>
{% endblock %}
